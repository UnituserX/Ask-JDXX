# --- START OF FILE app/services/__init__.py ---
# This file makes the 'services' directory a Python package.
# You can optionally import services here to make them available
# directly via app.services.service_name, but it's often cleaner
# to import them explicitly where needed (e.g., from app.services.openai_service import ...)
# --- END OF FILE app/services/__init__.py ---